// Remove in favour for NpcValues
export interface StudentValues {
    adjective: string[],
    race: string[],
    campus: string[],
    location: string[],
    backstory: string[]
}

// Remove in favour for NpcValues
export interface Student {
    adjective: string,
    race: string,
    campus: string,
    location: string,
    backstory: string
}