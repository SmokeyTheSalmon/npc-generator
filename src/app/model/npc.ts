export interface NpcValues {
    adjective: string[],
    species: string[],
    category: string[],
    location: string[],
    backstory: string[]
}

export interface Npc {
    adjective: string,
    species: string,
    category: string,
    location: string,
    backstory: string
}